{
  "name": "classroom-schedule-root",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm run migrate && npm run setup-admin",
    "migrate": "cd backend && node config/migrate.js",
    "setup-admin": "cd backend && node config/setupAdmin.js",
    "test-connection": "node test-db.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
